import { createTheme } from '@mui/material';

const theme = createTheme({
    palette : {
        primary : {
            main : '#294421'
        },
        secondary: {
            main: '#edf2ff',
        },
    }
});

export default theme